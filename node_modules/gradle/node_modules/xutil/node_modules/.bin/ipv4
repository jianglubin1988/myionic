#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_ipv4@0.0.4@ipv4/bin/ipv4" "$@"
  ret=$?
else 
  node  "$basedir/../../../_ipv4@0.0.4@ipv4/bin/ipv4" "$@"
  ret=$?
fi
exit $ret
